services:
  - type: web
    name: api-flow-recorder
    env: docker
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 3000
      - key: GEMINI_API_KEY
        sync: false
